{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the Harbor main window. Custom app commands are always available; this grants the plugin permissions the UI relies on.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "opener:default",
    "os:default",
    "fs:default",
    "updater:default",
    "process:allow-restart"
  ]
}
